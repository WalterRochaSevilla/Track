# ── Existing variables (copy your current .env structure above this line) ──

# ── Track C additions ──────────────────────────────────────────────────────
PORT=3000
JWT_SECRET=replace_with_a_secure_random_string_minimum_32_chars
UPLOAD_DIR=./uploads
DATABASE_URL=postgresql://user:password@localhost:5432/dbname